# 复制为 .env.local 并按需修改（.env.local 已被 gitignore，不会进入仓库）。
# 不配置也可以运行：除 VOTER_HASH_SECRET 外均有安全默认值。

# 社区投票指纹哈希的密钥。生产环境请换成足够长的随机串；
# 本地开发可用占位值（服务端默认即如此）。
VOTER_HASH_SECRET=replace-with-a-long-random-string

# 允许跨域访问 API 的页面来源，逗号分隔。
# ALLOWED_ORIGINS=http://127.0.0.1:5173,http://localhost:5173

# API 监听地址与端口（默认 127.0.0.1:8787）。
# API_HOST=127.0.0.1
# API_PORT=8787

# SQLite 数据目录（默认 .data/）。
# DATA_DIRECTORY=.data

# 覆盖默认的 opencode 模型（默认 opencode/deepseek-v4-flash-free）。
# 例如免费额度用尽时切换到付费通道：OPENCODE_MODEL=opencode-go/deepseek-v4-flash
# OPENCODE_MODEL=

# 前端构建时指向 API 的绝对地址（GitHub Pages 等静态部署需要）。
# VITE_API_BASE_URL=https://your-api.example.com
